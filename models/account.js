class Account {
    constructor(id, accountName, openingBalance) {
        this.id = id;
        this.accountName = accountName;
        this.openingBalance = openingBalance;
    }
}

export default Account;
